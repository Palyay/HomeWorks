<Window x:Class="Task18_1.NewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task18_1"
        mc:Ignorable="d"
        Title="О программе" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row="0"
            Text="Простой текстовый редактор"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold" />
        <Image Grid.Row="1" Source="D:\Курс Автоматизация\Проекты\Task18_1\img\Task_02.png"/>
        <Grid Grid.Row="2" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Content="Автор: Иванов Иван Иванович"
                   HorizontalAlignment="Left" 
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"/>
            <Label Grid.Row="1"
                   Content="Версия: 1.0"
                   HorizontalAlignment="Left" 
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"/>
            <Label Grid.Row="2"
                   Content="Дата создания: 01.01.2025"
                   HorizontalAlignment="Left" 
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"/>
        </Grid>
        <Button Grid.Row="4"
                Content="Закрыть"
                Width="100"
                Margin="10"
                Padding="10"
                Click="buttonClose_Click"                
                />
    </Grid>
</Window>
